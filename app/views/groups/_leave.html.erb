<%= form_tag current_user.memberships.find_by_group_id(@group),
             :html => { :method => :delete },
 			 :remote => true do %>
  <div class="actions"><%= submit_tag "Leave" %></div>
<% end %>